package com.training.ifaces;

public interface Function {

	double apply(double arg);
	//{} no need
	//by default all the methods are public and abstract
	//green public 
	//class can intantiated 
	
}
